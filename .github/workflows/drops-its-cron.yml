name: drops-its-cron
# Le cron a ete RETIRE (test cron_removed, 2026-07-21) : Cronwake doit annoncer UNE fois
# "monitoring stopped: the workflow no longer has a cron schedule".
on:
  workflow_dispatch:
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - run: echo "le bloc schedule a ete retire pour tester cron_removed"
